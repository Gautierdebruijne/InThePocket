import React from "react"
import Layout from "../components/layout"

const Menu = () => {
    return (
        <Layout>
            This is the menu template
        </Layout>
    )
}

export default Menu